import type{ReactNode}from'react';import{X}from'../shared/Icons';export function Modal({title,onClose,children}:{title:string;onClose:()=>void;children:ReactNode}){return <div role="dialog" aria-modal="true" aria-label={title} style={{position:'fixed',inset:0,zIndex:20,background:'#0004',display:'grid',placeItems:'center',padding:16}} onMouseDown={onClose}><div className="card" style={{width:'min(470px,100%)',padding:22,boxShadow:'0 18px 55px #0002'}} onMouseDown={e=>e.stopPropagation()}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',marginBottom:18}}><h2 className="serif" style={{margin:0,fontSize:22}}>{title}</h2><button onClick={onClose} aria-label="Close" style={{border:0,background:'transparent'}}><X size={19}/></button></div>{children}</div></div>}
