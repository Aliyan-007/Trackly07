import{Page}from'../layouts/AppLayout';import{useTracklyStore}from'../stores/useTracklyStore';import{BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer,PieChart,Pie,Cell}from'recharts';
export function Analytics(){const{tasks,habits,schedule}=useTracklyStore();const done=tasks.filter(x=>x.status==='done').length,open=tasks.filter(x=>x.status!=='done').length,checkins=habits.reduce((a,h)=>a+h.completed.length,0);if(!tasks.length&&!habits.length&&!schedule.length)return <Page title="Insights" eyebrow="Built from your activity"><section className="card empty-workspace"><h2 className="serif">Your story starts here.</h2><p>Complete a task, check in with a habit, or schedule a study session. Your private insights will grow from your own data.</p></section></Page>;const weekly=['Mon','Tue','Wed','Thu','Fri','Sat','Sun'].map((day,i)=>({day,value:tasks.filter(t=>t.status==='done'&&new Date(t.createdAt).getDay()===(i+1)%7).length}));const status=[{name:'Done',value:done,color:'#54a580'},{name:'Open',value:open,color:'#df7c62'}];return <Page title="Insights" eyebrow="Built from your activity"><div style={{display:'grid',gridTemplateColumns:'1.45fr 1fr',gap:18}} className="grid-collapse"><section className="card" style={{padding:22}}><h2 style={{fontSize:15,margin:0}}>Weekly momentum</h2><div style={{height:250}}><ResponsiveContainer><BarChart data={weekly}><XAxis dataKey="day"/><YAxis hide/><Tooltip/><Bar dataKey="value" fill="#292a29" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div></section><section className="card" style={{padding:22}}><h2 style={{fontSize:15,margin:0}}>Task balance</h2><div style={{height:210}}><ResponsiveContainer><PieChart><Pie data={status} dataKey="value" innerRadius={50} outerRadius={70}>{status.map(x=><Cell key={x.name} fill={x.color}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div></section></div><section style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:14,marginTop:18}} className="grid-collapse">{[['Completed tasks',String(done),'recorded'],['Habit check-ins',String(checkins),'recorded'],['Planned sessions',String(schedule.length),'on your timetable']].map(x=><div className="card" style={{padding:20}} key={x[0]}><div className="muted" style={{fontSize:12}}>{x[0]}</div><div className="serif" style={{fontSize:28,marginTop:9}}>{x[1]}</div><div className="muted" style={{fontSize:12}}>{x[2]}</div></div>)}</section></Page>}
