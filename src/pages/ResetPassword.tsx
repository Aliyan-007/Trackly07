import{useState}from'react';import{Link}from'wouter';import{useAuth}from'../contexts/AuthContext';export function ResetPassword(){const{sendPasswordReset,updatePassword,configured}=useAuth();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[reset,setReset]=useState(false),[message,setMessage]=useState('');return <main className="auth-page"><section className="auth-panel"><h1 className="serif">Reset password</h1><form className="auth-form" onSubmit={async e=>{e.preventDefault();const err=reset?await updatePassword(password):await sendPasswordReset(email);setMessage(err||'Done. Check your email or sign in with your new password.')}}>{reset?<input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" required/>:<input className="input" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" required/>}<button disabled={!configured} className="btn btn-primary">{reset?'Save password':'Send reset link'}</button></form><p className="form-message">{message}</p><button onClick={()=>setReset(!reset)} className="btn btn-soft">I opened the reset link</button><p><Link href="/sign-in">Back to sign in</Link></p></section></main>}
