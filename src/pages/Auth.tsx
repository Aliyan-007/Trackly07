import logo from '../assets/trackly-logo.png';import{useState}from'react';import{Link,useLocation}from'wouter';import{useAuth}from'../contexts/AuthContext';
const withTimeout=<T,>(promise:Promise<T>,ms=15000)=>Promise.race([promise,new Promise<T>((_,reject)=>window.setTimeout(()=>reject(new Error('Sign-in took too long. Check your internet connection and Supabase settings, then try again.')),ms))]);
export function AuthPage({mode}:{mode:'sign-in'|'sign-up'}){const auth=useAuth(),[,nav]=useLocation();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[fullName,setFullName]=useState(''),[role,setRole]=useState('Student'),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);const isSignUp=mode==='sign-up';const submit=async(e:React.FormEvent)=>{e.preventDefault();if(busy)return;setMessage('');if(isSignUp&&password!==confirm){setMessage('Passwords do not match. Please try again.');return}setBusy(true);try{const error=await withTimeout(isSignUp?auth.signUp(email,password,fullName,role):auth.signIn(email,password));if(error){setMessage(error);return}if(!isSignUp){nav('/app');return}fetch('/.netlify/functions/welcome',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,name:fullName})}).catch(()=>undefined);nav('/app')}catch(error){setMessage(error instanceof Error?error.message:'Unable to sign in. Please try again.')}finally{setBusy(false)}};const google=async()=>{setBusy(true);setMessage('');try{await withTimeout(auth.signInWithGoogle(),12000)}catch(error){setMessage(error instanceof Error?error.message:'Google sign-in could not start.')}finally{setBusy(false)}};return <main className="auth-page"><section className="auth-panel"><Link href="/" className="brand"><img src={logo} className="brand-logo" alt="Trackly logo"/>Trackly</Link><div className="auth-copy"><span className="eyebrow">PLAN · TRACK · ACHIEVE</span><h1>{isSignUp?'A clearer semester starts here.':'Welcome back.'}</h1><p>{isSignUp?'Create a personal space for your classes, work, and small daily wins.':'Sign in and pick up exactly where you left off.'}</p></div><button className="btn btn-google" type="button" disabled={!auth.configured||busy} onClick={google}><span className="google-dot">G</span>Continue with Google</button><div className="auth-divider"><span/>or continue with email<span/></div><form onSubmit={submit} className="auth-form">{isSignUp&&<><label htmlFor="full-name">Full name<input id="full-name" name="fullName" autoComplete="name" className="input" placeholder="How should we call you?" value={fullName} onChange={e=>setFullName(e.target.value)} required/></label><label htmlFor="role">Role<select id="role" name="role" className="input" value={role} onChange={e=>setRole(e.target.value)}><option>Student</option><option>Self-learner</option><option>Teacher</option><option>Parent</option></select></label></>}<label htmlFor="email">Email address<input id="email" name="email" autoComplete="email" className="input" type="email" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label htmlFor={isSignUp?'new-password':'password'}>Password<input id={isSignUp?'new-password':'password'} name={isSignUp?'newPassword':'password'} autoComplete={isSignUp?'new-password':'current-password'} className="input" type="password" placeholder="At least 6 characters" minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required/></label>{isSignUp&&<label htmlFor="confirm-password">Confirm password<input id="confirm-password" name="confirmPassword" autoComplete="new-password" className="input" type="password" placeholder="Repeat your password" minLength={6} value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label>}<button disabled={!auth.configured||busy} className="btn btn-primary auth-submit" type="submit">{busy?'Just a moment…':isSignUp?'Create my workspace':'Sign in to Trackly'}</button></form>{message&&<p className="form-message">{message}</p>}{!auth.configured&&<p className="muted tiny">Add Supabase environment variables to enable secure authentication.</p>}<p className="auth-switch">{isSignUp?'Already have an account?':'New to Trackly?'} <Link href={isSignUp?'/sign-in':'/sign-up'}>{isSignUp?'Sign in':'Create an account'}</Link></p></section></main>}
