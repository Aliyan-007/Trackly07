import logo from '../assets/trackly-logo.png';import{useState}from'react';import{Link,useLocation}from'wouter';import{useAuth}from'../contexts/AuthContext';
export function AuthPage({mode}:{mode:'sign-in'|'sign-up'}){const auth=useAuth(),[,nav]=useLocation();const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[fullName,setFullName]=useState(''),[role,setRole]=useState('Student'),[message,setMessage]=useState(''),[busy,setBusy]=useState(false);const isSignUp=mode==='sign-up';const submit=async(e:React.FormEvent)=>{e.preventDefault();setMessage('');if(isSignUp&&password!==confirm){setMessage('Passwords do not match. Please try again.');return}setBusy(true);const err=isSignUp?await auth.signUp(email,password,fullName,role):await auth.signIn(email,password);if(!err&&isSignUp){fetch('/.netlify/functions/welcome',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,name:fullName})}).catch(()=>undefined)}setBusy(false);if(err)setMessage(err);else if(!isSignUp)nav('/app');else setMessage('Check your inbox to confirm your email, then sign in.')};return <main className="auth-page"><section className="auth-panel"><Link href="/" className="brand"><img src={logo} className="brand-logo" alt="Trackly logo"/>Trackly</Link><div className="auth-copy"><span className="eyebrow">PLAN · TRACK · ACHIEVE</span><h1>{isSignUp?'A clearer semester starts here.':'Welcome back.'}</h1><p>{isSignUp?'Create a personal space for your classes, work, and small daily wins.':'Sign in and pick up exactly where you left off.'}</p></div><button className="btn btn-google" disabled={!auth.configured||busy} onClick={()=>auth.signInWithGoogle().catch(e=>setMessage(e.message))}><span className="google-dot">G</span>Continue with Google</button><div className="auth-divider"><span/>or continue with email<span/></div><form onSubmit={submit} className="auth-form">{isSignUp&&<><label>Full name<input className="input" placeholder="How should we call you?" value={fullName} onChange={e=>setFullName(e.target.value)} required/></label><label>Role<select className="input" value={role} onChange={e=>setRole(e.target.value)}><option>Student</option><option>Self-learner</option><option>Teacher</option><option>Parent</option></select></label></>}<label>Email address<input className="input" type="email" placeholder="you@example.com" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input className="input" type="password" placeholder="At least 6 characters" minLength={6} value={password} onChange={e=>setPassword(e.target.value)} required/></label>{isSignUp&&<label>Confirm password<input className="input" type="password" placeholder="Repeat your password" minLength={6} value={confirm} onChange={e=>setConfirm(e.target.value)} required/></label>}<button disabled={!auth.configured||busy} className="btn btn-primary auth-submit" type="submit">{busy?'Just a moment…':isSignUp?'Create my workspace':'Sign in to Trackly'}</button></form>{message&&<p className="form-message">{message}</p>}{!auth.configured&&<p className="muted tiny">Add Supabase environment variables to enable secure authentication.</p>}<p className="auth-switch">{isSignUp?'Already have an account?':'New to Trackly?'} <Link href={isSignUp?'/sign-in':'/sign-up'}>{isSignUp?'Sign in':'Create an account'}</Link></p></section></main>}
